extends layout 

block content
    h1= title
    -let items =results;
    div(class="container-fluid") 
      div(class="row m-1") 
       - let destination1 = `dispatch(\"/devil/create\")`
       button.btn( class="btn-info ml-1", onclick= destination1) Create
       each item in items
        - let destination = `dispatch(\"/devil/update/?id=${item._id}\")`
        - let destination2 = `dispatch(\"/devil/detail/?id=${item._id}\")`
        - let destination3 = `dispatch(\"/devil/delete/?id=${item._id}\")` 
        div(class="row-sm-2", class="m-1") Item #{item.devil_name}(#{item.version}) is #{item.type} 
          button.btn( class="btn-primary ml-1", onclick= destination) Update
          button.btn( class="btn-secondary ml-1", onclick= destination2) Detail
          button.btn( class="btn-warning ml-1", onclick= destination3) Delete 
    script. 
      function dispatch(path){ 
        window.location.href=path 
      }